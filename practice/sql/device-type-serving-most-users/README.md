# Device Type Serving Most Users

*One device type serves more users than the rest.*

[SQL · Medium · on DataDriven](https://datadriven.io/problems/device_type_serving_most_users)

| | |
|---|---|
| Solved | 2026-06-30 |
| Query complexity | O(n log n) (optimal O(n²)) |

The accepted solution is in [`solution.sql`](./solution.sql).
