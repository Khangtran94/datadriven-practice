# The Ones We Watch

*Only the services still under watch count. Rank them by where the errors gather.*

[SQL · Medium · on DataDriven](https://datadriven.io/problems/the_ones_we_watch)

| | |
|---|---|
| Solved | 2026-08-27 |
| Query complexity | O(n²) (optimal O(n log n)) |

The accepted solution is in [`solution.sql`](./solution.sql).
